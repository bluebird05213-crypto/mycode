import React,{useEffect,useState}from'react';import{FileImage,LoaderCircle,X}from'lucide-react';import{renderStoredPage}from'./pdfStore.js';
import'./zoom.css';
export default function OriginalPage({fileId,pageNumber,crop,compact=false,title}){const[src,setSrc]=useState(null),[error,setError]=useState(false),[zoom,setZoom]=useState(false);useEffect(()=>{let live=true;setSrc(null);setError(false);setZoom(false);renderStoredPage(fileId,pageNumber,crop).then(x=>live&&(x?setSrc(x):setError(true))).catch(()=>live&&setError(true));return()=>{live=false}},[fileId,pageNumber,crop?.top,crop?.bottom]);useEffect(()=>{if(!zoom)return;const close=e=>e.key==='Escape'&&setZoom(false);window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close)},[zoom]);const image=src?<img className="zoomable" src={src} alt={`原文件第 ${pageNumber} 页`} role="button" tabIndex="0" onClick={()=>setZoom(true)} onKeyDown={e=>(e.key==='Enter'||e.key===' ')&&setZoom(true)}/>:error?<p>原 PDF 页面数据不可用，请重新导入文件。</p>:<p><LoaderCircle className="spin"/>正在载入原页…</p>;const modal=zoom&&src?<div className="image-zoom-layer" onClick={()=>setZoom(false)}><div className="image-zoom-panel" onClick={e=>e.stopPropagation()}><button className="image-zoom-close" onClick={()=>setZoom(false)}><X/>关闭</button><img src={src} alt={`放大查看原文件第 ${pageNumber} 页`}/></div></div>:null;return compact?<>{<details className="original-backup"><summary>{title||'查看原题整页 PDF（备用）'}</summary>{image}</details>}{modal}</>:<><div className="original-page focused"><div><FileImage/><b>{title||'当前题目图形与选项（点击放大）'}</b><span>原 PDF 第 {pageNumber} 页</span></div>{image}</div>{modal}</>}
