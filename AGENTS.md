# mycode 项目协作规范

这是个人本地题库练习网站。代码和文档可以提交到 GitHub；个人 PDF、浏览器 IndexedDB/localStorage 数据和学习记录不得提交。

## 修改前

- 先阅读 `README.md`、`docs/ARCHITECTURE.md` 和当前 Git 状态。
- 保持题库解析、题目展示、学习数据和助手之间的边界，不为了小功能重写解析器。
- PDF 解析相关修改必须考虑答案泄露、跨页图形和旧题库迁移。

## 常用验证

```powershell
pnpm build
pnpm dev
```

开发服务器使用 `http://127.0.0.1:5173/`。涉及题目展示时，需要在浏览器中检查题干、选项、答案安全裁切和控制台错误。

## Git 规则

- `main` 是稳定分支，`dev` 用于开发。
- 功能完成且构建/浏览器回归通过后再提交和推送。
- 稳定里程碑创建版本标签，例如 `v0.2.0`。
- 不使用 force push，不把真实题库 PDF 或本机学习数据加入 Git。
- 功能改变时同步更新中文 README 或 `docs/` 文档。

